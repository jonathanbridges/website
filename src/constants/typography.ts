/** Responsive caption/eyebrow type — 12px → 13px (sm) → 14px (md+) */
export const eyebrowBase =
  "text-xs font-semibold uppercase leading-snug sm:text-[0.8125rem] md:text-sm";

export const eyebrowMuted = `${eyebrowBase} text-muted`;

export const eyebrowPrimary = `${eyebrowBase} text-primary`;
